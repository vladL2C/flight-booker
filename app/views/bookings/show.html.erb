<div class="row">
  <div class="col-md-6 col-md-offset-3">
 <table class="table table-hover"> 
  <%= render('booked_flight') %>
  <table class="table table-hover"> 
  <th>Passengers</th>
  <tbody>
    <% @booking.passengers.each do |passenger| %>
    <%= render partial: ('passengers'), locals: {passenger: passenger} %>
    <% end %>
  </tbody>
  </table>

  
  </div>
 </div>
